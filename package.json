{
  "name": "unified-ticketing",
  "version": "1.0.0",
  "private": true,
  "description": "Unified Helpdesk Ticketing Hub",
  "scripts": {
    "dev": "concurrently \"pnpm --filter api run start:dev\" \"pnpm --filter web run dev\"",
    "seed": "pnpm --filter api run seed",
    "dev:api": "pnpm --filter api run start:dev",
    "dev:web": "pnpm --filter web run dev",
    "build": "pnpm --filter api run build && pnpm --filter web run build",
    "build:api": "pnpm --filter api run build",
    "build:web": "pnpm --filter web run build",
    "verify": "pnpm --filter api run verify && pnpm --filter web run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
